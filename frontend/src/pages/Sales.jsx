import ComingSoonPage from "../components/ui/ComingSoonPage"

export default function Sales() {
  return (
    <ComingSoonPage
      title="Sales"
      subtitle="Pipeline, deals, and client accounts."
      description="Sales pipeline tracking isn't built yet — this page is reserved for the Sales Head role."
    />
  )
}
